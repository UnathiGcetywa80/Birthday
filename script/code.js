
function calculatebirthDay() {
    const birthDate = new date(document.getElementById('birthDate')).value
    const yourname = new Name(document.getElementById('name and surname')).value
    const nextbirthday = new Date(currentDate.getFullYear(), birthDate.getMonth(), birhday.getdate());
}
